spring:
  application:
    name: yorkdale
  jpa:
    open-in-view: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

#Port
server:
  port: 8084
